<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppCompatTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textColor">@color/primaryTextColor</item>
        <item name="android:actionBarTheme">@color/primaryTextColor</item>
    </style>

    <!-- My Application Theme -->
    <style name="MyTheme" parent="android:Theme.Material.Light.DarkActionBar">
        <item name="android:textColor">@color/primaryTextColor</item>
        <item name="android:button">@color/primaryColor</item>
        <item name="android:background">@color/primaryColor</item>
        <item name="android:actionBarTheme">@color/primaryTextColor</item>
    </style>
</resources>
